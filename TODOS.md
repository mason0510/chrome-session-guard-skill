# TODOS

## 当前版本待办
- [ ] 增加更细的退出码说明。
- [ ] 增加 `list-instances` 只读命令评估。
- [ ] 增加更多 shell smoke test。
- [ ] 评估是否要发布到 skills 生态安装器。

## 暂不做
- [ ] 不做自动实例选择。
- [ ] 不做 CDP 自动发现。
- [ ] 不做跨平台承诺。
- [ ] 不做隐式扫描任意 `user-data-dir`。

## 已知限制
- [ ] `list-profiles` 只读取默认 Chrome Local State。
- [ ] 显式 `--profile` 当前只支持 `new-window`。
- [ ] 默认模式依赖 AppleScript，与桌面会话强耦合。
